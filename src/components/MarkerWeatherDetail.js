import React from 'react';
// import Cards from "./Cards"

function WeatherDetail({onMarkerSelect}){

    if(!onMarkerSelect) {
        return <h3>Loading</h3>
    }

    // console.log('Selected Marker',onMarkerSelect)
    return(
        <div>
        
        </div>
    )
}

export default WeatherDetail